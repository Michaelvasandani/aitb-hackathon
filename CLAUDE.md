# aitb-library

> **Governed by [The Library](https://github.com/Brain-Bridge-AI/the-library).** These skills are not standalone: install the catalog skill (`git clone https://github.com/Brain-Bridge-AI/the-library.git ~/.claude/skills/library`), then pull skills with `/library use <name>`.
